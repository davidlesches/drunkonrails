%h1 Editing Blog Post

= render 'form'

.buttons
  = link_to 'View Post', admin_post_path(@post), class: 'btn btn-primary'
  = link_to 'Delete Post', admin_post_path(@post), :method => :delete, :confirm => "Are you sure?", class: 'btn btn-danger'
